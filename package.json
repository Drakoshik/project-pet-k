{
  "name": "project-pet-k",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run start",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "first:setup": "cd backend && npm run first:setup"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}